#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int count_letters(string text);

int main(void)
{
    string text = get_string("Text: ");
    letters = count_letters(text);
    printf("%i\n", letters);
}

int count_letters(string text)
{

}