#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long credit_number = get_long("Number: ");
    do
    {
        credit_num = get_long("Number: ");
    }
    while
    {
        credit_num != 
    }
}